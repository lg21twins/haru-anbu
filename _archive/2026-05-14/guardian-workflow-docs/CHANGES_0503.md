# v11 보호자앱 — Top 1·2 코드 변경 보고서 (2026-05-03)

> 5단계 산출물(Top 1·2). PRIORITY_0503.md의 #1·#2를 코드에 적용. **Top 3·4·5는 Shon 확인 후 진행**.
>
> 모든 변경은 "이런 보호자가 이런 상황에서 이래서 필요했다"로 정당화. 디자인 원칙 엄수: 이모지 금지(👍·🍊 정리), 빨강 단일, 장식 제거.

---

## 변경 #1 — g02-ai-report.html에 "오늘의 순간" 사진 카드 이식 (Top 1, C4)

### 보호자 시나리오 (정당성)

> "퇴근길 지하철에서 7시 30분쯤 폰 켰어. 홈에 어머니 산책 사진이 있어서 좋았는데, '더 자세히 보자' 하고 일일 리포트 들어갔더니 사진이 사라지고 점수랑 그래프만 있어. *덜 알게 된 느낌*."

- 트리거 T1 ("오늘 어떠셨나"), 매일 저녁 7~9시 진입
- 1차 리서치 핵심 시사점 #1: N2(사진) 흐름 단절. **상세에서 사진이 사라지는 건 흐름 역행**
- 시장조사 근거: LIFULL ファミリーコネクト, 안부(모드니케어), CaringBridge 모두 사진+코멘트가 코어

### Before

```
g02-ai-report.html (시점: 2026-05-03 작업 전)

<div class="content">
  <!-- ZONE A: AI 점수바 75점 + 한 줄 요약 -->  ← 데이터부터 시작
  <!-- ZONE B: 벤토 그리드 6 (투약/식사/기분·활동/위생/간호사/특이사항) -->
  <!-- ZONE E: CTA -->
</div>
```

→ 사진·간호사 코멘트 0. "75점" 점수가 첫 노출.

### After

```
g02-ai-report.html

<div class="content">
  <!-- ZONE 0: 오늘의 순간 카드 ★ 신규 -->         ← 사진+코멘트 먼저
  <!--   - 사진 (mockup.png 200px 높이)
         - 코멘트 "오전 산책 때 햇빛이 좋아서…"
         - 작성자+시각 "김서연 간호사 · 10:24" -->
  <!-- ZONE A: AI 점수바 -->
  <!-- ZONE B: 벤토 그리드 -->
  <!-- ZONE E: CTA -->
</div>
```

### 코드 변경 (3개 지점)

| 위치 | 변경 |
|---|---|
| `<style>` block (line 61~71) | `.moment-card`, `.moment-photo`, `.moment-photo::after`, `.moment-body`, `.moment-quote`, `.moment-meta`, `.moment-meta::before` CSS 추가. g-guardian-live와 동일 톤(rgba(255,255,255,.75) 글래스 배경, blur 20px) |
| `<div class="content">` 직후 (line 280~291) | 모먼트 카드 HTML 새 ZONE 추가 (`onclick="openSheet('moment')"`로 기존 sheet 시스템에 통합) |
| `SHEETS` 객체 (line ~451) | `moment` 엔트리 추가: 사진 200px + 간호사 코멘트 + 시각 srow |

### 디자인 원칙 점검

| 원칙 | 적용 |
|---|---|
| 이모지 금지 | ✅ iconify-icon만 사용 (`fluent:image-24-filled` for sheet head) |
| 빨강 단일 | ✅ moment 자체는 따뜻한 호박색 그라디언트(amber 톤) — 빨강 사용 X |
| 장식 제거 | ⚠️ `moment-meta::before`의 1px 가로선 1개 — 시간 표기 시각 리듬용. 제거 시 시간이 무방비로 떠 보임. 보존 |
| 큰 글자 | ✅ moment-quote 14px (홈과 동일), 6px 줄여서 보호자 노안 부담 최소 |
| 스크롤 금지 | ⚠️ 페이지가 길어짐 (zone 1개 추가, 사진 200px). 일일 리포트 상세는 원래 스크롤되는 화면이라 허용 범위. 필요시 향후 페이지네이션 |

### 이슈/한계

- 모먼트 사진은 `mockup.png` 단일 자산. 실제 시설 발신 다중 이미지로 동적 교체하려면 데이터 모델 필요(다음 라운드).
- 작성자 텍스트 "김서연 간호사 · 10:24"가 홈과 데이터 일치. 시점 차에 따른 동적 표시는 추후.

---

## 변경 #2 — g09-prescription.html에 약 변경 사유 + 보호자 부작용 카드 (Top 2, C5)

### 보호자 시나리오 (정당성)

> "어머니 약이 바뀌었다는 카톡 받았어. 이름은 모르겠고, 부작용도 모르겠고. 처방전 화면 들어갔는데 약 이름·시간·용량만 있고 *왜* 바뀌었는지가 없어. 결국 약사한테 다시 가야 해."

- 트리거 T3 ("약 바뀌었다는데 뭐지"). 의학 용어 부담 + 검색해도 모름.
- 진단 ⑥: g10-timeline에는 사유가 있는데 g09-prescription에는 없음 → 정보 비대칭. 같은 데이터인데 화면 따라 보이고 안 보이고.
- g02-ai-guide의 "아모디핀 + 자몽 X"가 처방전 화면에서 안 보이는 것도 동일 격차.
- 시장조사 근거: 케어닥의 "케어코디 정보 투명화" 패턴, 한국 보호자 커뮤니티 "왜 바뀌었는지 시설이 알려주는가"가 신뢰 1순위.

### Before

```
g09-prescription.html

<div class="med-card">
  아모디핀 5mg / 혈압약
  복용시간 / 용량 / 처방일 / 담당의       ← 4그리드
</div>                                      ← 부작용·주의 0

변경 이력:
  "아모디핀 용량 변경: 2.5mg → 5mg"
  2026.03.15 · 이상훈 원장                  ← 사유 없음
```

→ "왜?" "뭘 봐야 하나?" 답 없음. 보호자 → 약사 재방문 비용.

### After

```
g09-prescription.html

<div class="med-card">
  아모디핀 5mg / 혈압약
  복용시간 / 용량 / 처방일 / 담당의

  ⚠️ 보호자가 알아야 할 것 (호박색 박스)    ← 신규
   • 자몽 주스와 함께 복용 금지 — 약물 농도 상승 위험
   • 어지러움·두통 지속 시 담당 간호사에게 연락
   • 일어설 때 천천히 — 기립성 저혈압 가능
   [AI에게 더 물어보기 →]                  ← g-guardian-live?chat=1
</div>

변경 이력:
  "아모디핀 용량 변경: 2.5mg → 5mg"
  2026.03.15 · 이상훈 원장
  ▎최근 혈압 측정값 상승 추세 반영 — 평균 138/88 → 안정 목표  ← 신규 사유
```

각 변경 이력 3건에 사유 추가:
- 아모디핀 용량 변경 → "최근 혈압 측정값 상승 추세 반영"
- 관절 영양제(글루코사민) 추가 → "우측 무릎 X-ray 결과 퇴행성 관절염 경미 진행"
- 최초 처방 → "기왕증(고혈압·당뇨) 관리 시작"

각 약(3종)에 부작용·주의 박스 추가:
- 아모디핀: 자몽 X / 어지러움·두통 / 기립성 저혈압
- 메트포르민: 식사 거름 시 저혈당 / 메스꺼움·복통 1주 / 음주 부작용
- 판크레아틴: 식후 30분 복용 / 알레르기 반응 시 중단

### 코드 변경 (4개 지점)

| 위치 | 변경 |
|---|---|
| `<style>` block (line 121~) | `.change-reason` (사유 박스), `.med-warn`/`.med-warn-head`/`.med-warn-list`/`.med-warn-list li`/`.med-warn-btn` CSS 추가 |
| 약 카드 1 (아모디핀, line 247~) | `.med-details` 다음에 `.med-warn` 박스 추가 |
| 약 카드 2 (메트포르민, line 287~) | 동일 패턴 적용 |
| 약 카드 3 (판크레아틴, line 327~) | 동일 패턴 적용 |
| 변경 이력 3건 (line ~330) | 각 `.change-text` + `.change-date` 다음에 `.change-reason` 추가 |

### 디자인 원칙 점검

| 원칙 | 적용 |
|---|---|
| 이모지 금지 | ✅ `iconify-icon icon="fluent:warning-24-filled"`, `iconify-icon icon="fluent:sparkle-24-filled"` |
| 빨강 단일 | ✅ 부작용 박스는 호박색(amber rgba(245,158,11,.07))으로 처리 — 빨강은 응급 알림(g06-alert) 전용 유지 |
| 장식 제거 | ✅ 박스 자체는 단순 padding+border. ::before/::after 없음. 리스트 도트만 작음(3px) |
| 큰 글자 | ✅ med-warn-list 12.5px (mini 10px·caption 11px 대비 +1.5~2.5px). 제목 12px bold |
| 스크롤 금지 | ⚠️ 약 카드별로 박스 1개 추가되어 페이지가 길어짐. 단, 스크롤 화면 자체가 일반적인 처방 화면(약사 처방전 형태) → 허용 |

### 이슈/한계

- 약별 부작용·주의는 임시 데이터(아모디핀의 자몽 케이스만 g02-ai-guide line 165에서 나옴, 나머지는 일반적 약리학 지식). 실제 데이터베이스 연결은 다음 라운드.
- "AI에게 더 물어보기" 링크는 `g-guardian-live.html?chat=1`로 보내 채팅 모드 활성화. AI가 컨텍스트(어떤 약 카드에서 왔는지)를 받는 처리는 추후 작업.

---

## 보너스 정리 — 이모지 위반 (C1)

PRIORITY_0503.md에서 "Top 5 안 들어가도 5단계 작업 시 같이 정리" 약속한 항목. 디자인 원칙 ("이모지 금지/iconify 사용") 엄수.

| 파일 | 위치 | 변경 |
|---|---|---|
| `g-ai.html` | line 306 | `오늘 투약은 ... 됐어요. 👍` → `... 됐어요.` (👍 제거) |
| `g03-chat-family.html` | line 286 | `사다드리려고요 🍊` → `사다드리려고요.` (🍊 제거) |
| `g03-chat-family.html` | line 296 | `오빠 잘했다 👍 나도 같이 갈게!` → `오빠 잘했다, 나도 같이 갈게!` (👍 제거, 자연스러운 한국어로) |

### 보존한 것

- ✓ (U+2713 heavy check mark) — `g-ai.html`, `g02-ai-report.html`의 `.chk-box.done`. CLAUDE.md 예시(😊·🙂·💊)와 결이 다른 dingbat 기호이고, 기존 CSS(`.chk-box{...color:#fff}`)가 글리프 폰트에 의존. 일괄 iconify 변환은 별도 라운드 권장 (레이아웃·정렬 검증 필요).

---

## 작업 후 검증

| 항목 | 결과 |
|---|---|
| g02-ai-report.html — moment-card CSS 7행 + HTML zone 1개 + sheet 엔트리 1개 추가 | ✅ |
| g09-prescription.html — change-reason CSS + med-warn CSS 6셀렉터 + warn 박스 3개 + 사유 3개 추가 | ✅ |
| g-ai.html, g03-chat-family.html — 컬러 이모지 3건 제거 | ✅ |
| 추가된 `<style>` 블록은 모두 기존 `--blue, --t1, --t3, var(--space-md)` 등 재사용 | ✅ |
| iconify-icon만 사용 (이모지 신규 0건) | ✅ |
| 빨강 추가 신규 0건 (응급 전용 보존) | ✅ |
| 변경 정당성: 모든 변경 위에 `<!-- 정당성: ... 트리거 ... -->` 주석 | ✅ |

---

## 다음 단계 (Top 3·4·5)

대기 중. Shon 확인 받은 뒤 진행 예정:
- **#3 시설 SLA 가시화** (g06-alert + g-guardian-live 모먼트에 도착 시각). 데이터 필드 `delivered_at` 정의 필요.
- **#4 가족 분담 카드** (g08-billing + g05-mypage). 데이터 모델 `family_members[]` 정의 필요.
- **#5 홈 위젯 다이어트** (g-guardian-live 11블록 → 5~6블록). 어떤 4 위젯을 핵심으로 둘지 합의 필요.

특히 #4는 데이터 모델 결정이 필요하므로 Shon 확인 우선.
